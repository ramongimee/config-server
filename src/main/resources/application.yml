
server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:      
          uri: https://github.com/ramongimee/microservice-repo/
          search-paths: fornecedor, loja, transportador